/**
 * Controller padrão de páginas
 */
define([
    'connecta'
], function(connecta) {
    
    return connecta.controller('AppPageController', [
        '$scope',
        function($scope) {
            
        }
    ]);
});