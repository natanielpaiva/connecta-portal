/**
 * Controller padrão de páginas
 */
define([
    'angular'
], function(angular) {

    var app = angular.module('connecta');
    app.controller('AppPageController', [
        '$scope',
        function($scope) {
            
        }
    ]);
});